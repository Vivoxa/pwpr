<div id="main-container" class="container">
<h1>Editing Admin</h1>
  <%= link_to 'Show', @admin, class: ['btn btn-sm btn-primary', 'show-btn-md'] %>
  <%= link_to 'Back', request.referer, class: ['btn btn-sm btn-primary', 'show-btn-md'] %>
  <div class="card">
    <h3 class="card-header"><%= @admin.name %></h3>
    <div class="card-block">
      <!--	<h4 class="card-title">Administration</h4>-->
      <p class="card-text">
      <%= render 'form' %>
      </p>
    </div>
    <div class="card-footer">
    </div>
  </div>
</div>
