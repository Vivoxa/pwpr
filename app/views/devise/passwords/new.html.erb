<%= title "Forgot your password?" %>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.email_field :email, placeholder: "Email", class: "devise-lgn", autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "Email reset password link", class: ["btn", "btn-info", "devise-btn"] %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
