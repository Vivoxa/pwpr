<%= form_for(@email_content_type) do |f| %>
    <% if @email_content_type.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@email_content_type.errors.count, "error") %> prohibited this email_name from being saved:</h2>

          <ul>
            <% @email_content_type.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field centre">
      <%= f.text_field :name, placeholder: 'Email Content Type', class: 'devise-lgn form-control' %>
    </div>
    <div class="actions centre">
      <%= f.submit class: 'btn btn-primary devise-btn' %>
    </div>
<% end %>

