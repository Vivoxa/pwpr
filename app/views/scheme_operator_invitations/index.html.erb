<p id="notice"><%= notice %></p>

<h1>Pending Scheme Operator Invitations</h1>

<table>
  <thead>
    <tr>
      <th>Recipient</th>
      <th>Date sent</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @scheme_operator_invitations.each do |invited_user| %>
      <tr>
        <td><%= invited_user.email %></td>
        <td><%= invited_user.invitation_sent_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Invite Scheme Operator', new_scheme_operator_invitation_path %>
