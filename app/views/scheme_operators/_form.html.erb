<%= form_for(@scheme_operator) do |f| %>
  <% if @scheme_operator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scheme_operator.errors.count, "error") %> prohibited this member from being saved:</h2>

      <ul>
        <% @scheme_operator.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field centre">
    <%= f.text_field :first_name, placeholder: 'Name', class: "devise-lgn form-control" %>
  </div>
  <div class="field centre">
    <%= f.text_field :email, placeholder: 'Email', class: "devise-lgn form-control" %>
  </div>
  <div class="field centre">
    <%= f.label :approved %>
    <%= f.select :approved, options_for_select([['Yes', true], ['No', false]], @scheme_operator.approved),{}, { class: "devise-lgn form-control" } %>
  </div>

  <div class="actions centre">
    <%= f.submit 'Save', class: ["btn", "btn-primary", "devise-btn"]  %>
  </div>
<% end %>
