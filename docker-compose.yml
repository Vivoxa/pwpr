version: '2'
services:
  app:
    build: .
    command: bash -c "bundle && bundle exec rails s -p 3000 -b '0.0.0.0'"
    tty: true
    stdin_open: true
    volumes:
      - .:/pp_pwpr
    ports:
      - "3000:3000"
